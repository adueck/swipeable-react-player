# swipeable-react-player
A basic example of react-player with swiping and keyboard shortcuts to seek back and forth

To run the demo:

```
git clone https://github.com/adueck/swipeable-react-player
cd swipeable-react-player
npm install
npm run dev
```
